spring.application.name=prueba-tecnica-test
spring.h2.console.enabled=true
spring.datasource.url=jdbc:h2:mem:testdb  # Base de datos en memoria para pruebas
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=antoniomoreno
spring.datasource.password=rafa0019
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop  # Deja que schema.sql gestione la creación de tablas
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema-test.sql  # Usar schema.sql de pruebas
spring.sql.init.data-locations=classpath:data-test.sql  # Usar data.sql de pruebas